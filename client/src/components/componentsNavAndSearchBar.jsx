const NavSearchBar = (props) => {
  return (
    <div>
      <h1>NavSearchBar</h1>

      <button>Feed</button>
      <button>Profile</button>
      <input type="text" />
      <button>Search</button>
    </div>
  );
};

export default NavSearchBar;
